auto-reload yes
reload-time 1
reload-threads 100

browser "firefox"
bind-key f macro-prefix
macro j set browser "mpv-script %u &" ; open-in-browser ; set browser "firefox"
macro d set browser "mpv-script %u &" ; goto-url 4 ; set browser "firefox"
macro l set browser "rtv %u" ; open-in-browser ; set browser "firefox"
macro o set browser "firefox %u" ; open-in-browser ; set browser "firefox"

# unbind keys
# unbind-key ENTER
unbind-key j
unbind-key k
unbind-key J
unbind-key K

# bind keys - vim style
bind-key j down
bind-key k up
bind-key l open
bind-key h quit
bind-key a quit

# proxy
use-proxy yes
proxy-type socks5h
proxy 127.0.0.1:9050
